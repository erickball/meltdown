// Re-export render module
export { PlantCanvas } from './canvas';
export { renderComponent, renderGrid, renderConnection, worldToScreen, screenToWorld } from './components';
export { getFluidColor, COLORS, getComponentStressColor } from './colors';
